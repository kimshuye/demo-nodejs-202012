const Welcomto = function (id, name) {
  this.id = id;
  this.name = name;
};

class Welcomto {
  constructor(id, name) {
    this.id = id;
    this.name = name;
  }
}

// const welcomto = new Welcomto(1, "World");

module.exports = Welcomto;
